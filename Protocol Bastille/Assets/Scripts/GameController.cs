using UnityEngine;
using System.Collections;

public class GameController : MonoBehaviour {
    public GameObject blackScreenFadeIn;
	// Use this for initialization
	void Start () {
        blackScreenFadeIn.SetActive(true);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
